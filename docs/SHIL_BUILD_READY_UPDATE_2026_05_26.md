# SHIL Build-Ready Update - 2026-05-26

## تغییرات اعمال‌شده

### 1. مسیر اجرای پروژه خورشیدی سناریومحور شد
- انتخاب مسیر «برق خورشیدی با پنل» اکنون مستقیم به صفحه انتخاب سناریوی خورشیدی می‌رود.
- صفحه سناریوی خورشیدی به ۶ مسیر اختصاصی تبدیل شد:
  - آفگرید
  - هیبرید
  - آنگرید
  - بکاپ اضطراری
  - پمپ خورشیدی
  - صنعتی
- هر سناریو مستقیم به صفحه «ورودی محاسبات اختصاصی» همان سناریو متصل است.

### 2. صفحه ورودی محاسبات اختصاصی اضافه شد
- فایل `src/data/solarScenarioInputConfig.js` اضافه شد.
- هر سناریو فیلدهای مخصوص خودش را دارد.
- خروجی مشتق‌شده هر سناریو شامل انرژی مبنا، توان مبنا و Surge در localStorage ذخیره می‌شود.
- موتور بار و مسیر سیستم از همین دیتای سناریویی تغذیه می‌شود.

### 3. بانک پنل خورشیدی اصلاح شد
- بانک پنل از حالت بازه‌ای حذف شد.
- هر توان پنل به‌صورت رکورد مستقل وارد شد.
- توان‌های مستقل اضافه‌شده:
  180, 190, 360, 370, 380, 430, 435, 440, 445, 450, 580, 585, 590, 595, 600, 645, 650, 655, 660, 665, 695, 700, 705, 710, 715, 720, 730, 735, 740, 745, 750W
- برند نمایشی همه پنل‌ها SHIL است.
- دیتای مهندسی هر رکورد شامل Vmp, Imp, Voc, Isc, Efficiency, NOCT, Temp Coefficients, Max Fuse و Max System Voltage است.

### 4. آیکون‌های قدیمی حذف شدند
- `public/icons/neon3d` حذف شد.
- فایل‌ها و فولدرهای خراب `#U06f1` حذف شدند.
- Referenceهای قدیمی neon3d در `src/mobile-ui` به مسیر جدید SHIL اصلاح شدند.

### 5. Android/Capacitor اصلاح شد
- `capacitor.config.json` با `applicationId` اندروید یکسان شد: `com.shil.engineering`
- MainActivity تکراری مسیر `com/shil/app` حذف شد.

### 6. اصلاح خطای سینتکس موجود
- شناسه خراب در `src/modules/ready-scenarios/readyScenarios.config.js` اصلاح شد.

## تست‌های انجام‌شده
- `npm run infra:check` اجرا شد و پاس شد.
- 619 فایل JS/JSX با TypeScript transpile syntax-check بررسی شدند و خطای سینتکس نداشتند.

## محدودیت تست Build
- `npm run build` به دلیل نبود `node_modules` داخل ZIP قابل اجرا نبود.
- `package-lock.json` نیز در فایل ورودی موجود نبود؛ بنابراین نصب وابستگی‌ها در محیط بدون اینترنت کامل انجام نشد.
- سورس از نظر importهای نسبی و syntax آماده است، اما برای Build نهایی باید `npm install --legacy-peer-deps` در محیط دارای اینترنت اجرا شود.
