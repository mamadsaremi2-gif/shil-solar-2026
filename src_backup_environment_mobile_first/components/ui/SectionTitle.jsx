import React from "react";

export default function SectionTitle({
  title,
  subtitle,
}) {
  return (
    <div className="section-title-v15">

      <h2>{title}</h2>

      <p>{subtitle}</p>

    </div>
  );
}
