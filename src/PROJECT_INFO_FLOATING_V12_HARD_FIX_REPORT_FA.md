# SHIL Project Info Floating V12 - Hard Fix

این نسخه اصلاح ساختاری صفحه «اطلاعات پروژه» است و صرفاً CSS جدید روی قوانین قدیمی سوار نمی‌کند.

## اصلاحات
- بلوک سه کارت دیگر از کلاس عمومی `shil-form-grid` استفاده نمی‌کند.
- بخش مشخصات اولیه دیگر از کلاس عمومی `shil-project-info-primary-fields` استفاده نمی‌کند؛ بنابراین والد سفید/خاکستری عمومی نمی‌تواند برگردد.
- Grid اختصاصی `shil-project-info-three-grid-v12` ایجاد شد.
- سه کارت کلاس‌های مستقل V12 دارند.
- کارت تاریخ با `grid-column: 1 / -1` و `justify-self: center` واقعاً در مرکز ردیف دوم قرار می‌گیرد.
- Rule قدیمی `#shil-project-info-root .shil-form-grid > .shil-field-card:last-child` که `justify-self:end` تحمیل می‌کرد از فایل parity حذف شد.
- قواعد قدیمی موقعیت کارت تاریخ در `project-info-requested-update-final.css` حذف شدند؛ آن فایل فقط رنگ/فرمت ورودی و دکمه را نگه می‌دارد.
- بلوک قدیمی V10 از `index.css` حذف شد.
- CSS جدید V12 آخرین import در `main.jsx` است.
- عنوان «مشخصات اولیه پروژه» روی بک‌گراند اصلی و بدون Card والد قرار می‌گیرد.
- نام پروژه `کاربر` و کارفرما `SHIL CO` حفظ شده‌اند.
- این صفحه مشترک هر دو مسیر خورشیدی و برق اضطراری است، بنابراین اصلاح در هر دو مسیر اعمال می‌شود.
