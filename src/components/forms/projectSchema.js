import { z } from "zod";

export const projectSchema = z.object({

  projectName:
    z.string()
      .min(3, "????? ? ???????"),

  clientName:
    z.string()
      .min(2, "??? ??????? ?????? ???"),

  city:
    z.string()
      .min(2, "??? ????? ?????? ???"),

  projectType:
    z.string()
      .min(1, "??? ????? ?????? ????"),

  dailyEnergy:
    z.coerce.number()
      .min(1, "???? ???????"),

  peakLoad:
    z.coerce.number()
      .min(1, "???? ???????"),

  batteryVoltage:
    z.string(),

  backupHours:
    z.coerce.number()
      .min(1),

  notes:
    z.string()
      .optional(),

});
