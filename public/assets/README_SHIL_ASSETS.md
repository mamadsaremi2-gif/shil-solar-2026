# SHIL asset slots

These files are intentionally referenced by the new mobile UI layer. Replace them with final brand assets using the exact names below:

- `/public/assets/shil-dashboard-bg.png` — full-screen dashboard/background master image.
- `/public/assets/shil-logo.png` — animated welcome logo.
- `/public/assets/shil-products.png` — contact page product banner.
- `/public/assets/qr-1.png` to `/public/assets/qr-4.png` — direct QR links.

Dashboard and New Project icons currently fall back to vector icons. If image icons are added later, keep them transparent PNG/WebP and wire them through `IosIconGrid` item `image` paths.
